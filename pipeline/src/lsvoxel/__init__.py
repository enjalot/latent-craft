"""lsvoxel: data pipeline for the voxel-UMAP explorer.

Turns existing embeddings + thumbnails for a dataset into a chunked, streamable
voxel pack (see docs in the project plan) consumable by the three.js frontend.
"""
